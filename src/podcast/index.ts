/**
 * Podcast module wrapper
 */

'use strict';

import * as Feed from './feed';
import * as Search from './search';
import * as Top from './top';

export const feed = Feed.feed;
export const search = Search.search;
export const top = Top.top;
