{
  "name": "podcst-api",
  "version": "0.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "lint": "tslint -c tslint.json --project tsconfig.json",
    "compile": "tsc",
    "pre": "yarn lint && yarn compile",
    "predev": "yarn pre",
    "prestart": "yarn pre",
    "dev": "pm2 start ecosystem.config.js && tsc -w",
    "start": "pm2 start build/index.js -i 0 --no-daemon"
  },
  "dependencies": {
    "axios": "^0.16.1",
    "hapi": "^16.1.1",
    "ioredis": "^2.5.0"
  },
  "devDependencies": {
    "@types/hapi": "^16.0.3",
    "@types/ioredis": "^0.0.23",
    "@types/node": "^7.0.13",
    "good": "^7.1.0",
    "good-console": "^6.4.0",
    "good-squeeze": "^5.0.2",
    "inert": "^4.2.0",
    "pm2": "^2.4.5",
    "tslint": "^5.1.0",
    "typescript": "^2.2.2"
  }
}
